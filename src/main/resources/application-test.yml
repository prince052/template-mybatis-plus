server:
  port: 9066

spring:
  #  配置数据源
  datasource:
    url: jdbc:mysql://mysql.svc:3306/qbank_compile_bak?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username:
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    com.beejeen.qbank.compile: info
  config: classpath:logback-test.xml
